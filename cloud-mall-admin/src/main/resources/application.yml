server:
  port: 9501
spring:
  profiles:
    active: dev
  application:
    name: cloud-mall-admin
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
management:
  endpoints:
    web:
      exposure:
        include: "*"
